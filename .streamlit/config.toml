[server]
# Sol disposition (29 Aug): dashboard is LOCAL-ONLY — bind to loopback so
# nothing else on the network can open it.
address = "127.0.0.1"

[theme]
base = "dark"
primaryColor = "#22d3ee"
backgroundColor = "#0b1020"
secondaryBackgroundColor = "#151b2e"
textColor = "#e7ecf5"
font = "sans serif"
